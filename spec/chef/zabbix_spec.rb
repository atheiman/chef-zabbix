require 'spec_helper'

describe Chef::Zabbix do
  it 'has a version number' do
    expect(Chef::Zabbix::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
